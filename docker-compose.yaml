version: "3.8"

services:
    nextjs:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "3333:3000"
        environment:
            DATABASE_URL: "file:./file.db"
        volumes:
            - ./prisma/file.db:/app/file.db
        restart: unless-stopped
